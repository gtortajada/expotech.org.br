'use client';

import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: "Ayar, Arial, sans-serif",
  headings: {
    fontFamily: "Ayar, Arial, sans-serif",
    sizes: {
      h1: { fontSize: "3rem", fontWeight: "700" },
      h2: { fontSize: "2.5rem", fontWeight: "300" },
    },
  },
}); 
